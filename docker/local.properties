# Mounted read-only over /workspace/local.properties inside the build container
# (see docker-compose.yml) so the containerised build always finds the SDK at the
# image's location, regardless of any host-specific local.properties.
sdk.dir=/opt/android-sdk
